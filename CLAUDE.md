# pricing-optimizer-api — backend context

Go backend for Pricing Optimizer. This is Git repo #1. When Claude Code is opened here,
this is the active scope (the `.git/` directory stops the upward search into the umbrella).
Cross-cutting product context lives in the umbrella `../.claude/CLAUDE.md`; the full
decision log is `../HANDOFF.md`.

## What this service does

Analyzes a product URL and generates three pricing-page variations via structured LLM
tool calling, streaming them over Server-Sent Events. See `openapi.yaml` (synced from the
umbrella root) for the contract. The 5 endpoints: analyze, generate (SSE), get generation,
export, healthz.

## Architecture — Clean Architecture, pragmatic four layers

```
cmd/api/            entry point, wiring, graceful shutdown
internal/
  domain/           entities and ports (interfaces); imports nothing
  usecase/          orchestration; imports domain only
  adapter/
    httpapi/        Chi handlers + SSE writer
    scraper/        chromedp (SPA) + colly (static)
    llm/            Anthropic + Groq adapters behind LLMProvider
    repository/     pgx + sqlc
    cache/          Redis client
  api/              generated from openapi.yaml (server.gen.go)
  config/           envconfig struct
  telemetry/        OpenTelemetry setup
db/
  migrations/       goose
  queries/          sqlc
test/
  e2e/              contract tests
  fixtures/         LLM golden responses
```

Layer rules (non-negotiable, enforced in review):
- `usecase` imports `domain` only. Never adapters, never external packages.
- `domain` imports nothing. Pure entities and interfaces.
- `adapter` imports `domain`. Never `usecase`.
- `cmd` imports everything and wires at startup.

## Stack

Go 1.23+ (installed: 1.26.5), go-chi/chi v5, oapi-codegen, chromedp + colly, official
anthropic-sdk-go, OpenAI-compatible client for Groq, go-playground/validator,
caarlos0/env + godotenv, log/slog, OpenTelemetry SDK, jackc/pgx v5, sqlc, pressly/goose,
redis/go-redis v9, testify, testcontainers-go, uber-go/mock, golangci-lint, air.

## Contract workflow (source of truth)

`openapi.yaml` is generated/owned at the umbrella root, synced here via `make sync-openapi`.
Never edit this repo's `openapi.yaml` directly. Regenerate server code with oapi-codegen
after a sync. Spec is OpenAPI 3.1; see `../docs/decisions/0004-openapi-31-with-oapi-codegen.md`
for the codegen caveat.

## Testing (non-negotiable)

- Table-driven tests with `t.Run` and descriptive English names. Helpers call `t.Helper()`.
- Mocks generated by uber-go/mock, never hand-written.
- Integration tests use testcontainers-go (real Postgres), not mocks.
- LLM adapter tests use mocked clients + golden fixtures in `test/fixtures/`.
- Coverage >= 80% in `internal/usecase` and `internal/domain`.

## Hard rules

- English everywhere in shipped artifacts. No emojis in commits/PRs/code. No `fmt.Println`
  in shipped code (use slog). Secrets only via env.
- Conventional Commits, enforced by commitlint + lefthook.
- Every non-trivial architectural decision gets an ADR in `../docs/decisions/`.

## Workflow

Spec-driven: `/speckit-constitution` -> `/speckit-specify` -> `/speckit-plan` ->
`/speckit-tasks` -> `/speckit-implement`. Constitution lives in `.specify/memory/constitution.md`.

## Branching workflow (constitution: branch-per-task)

`main` is protected — never develop or commit on it directly. Create the branch BEFORE
writing code, from an up-to-date `main`:
- Spec-driven feature: `NNN-slug`, created by `/speckit-specify` (e.g. `002-generate-variations`).
  Never rename it; `specs/NNN-slug/` must match.
- Standalone change (CI, deps, tooling, docs, hotfix): `<type>/<slug>`, Conventional type +
  kebab-case slug (e.g. `ci/golangci-lint-workflow`).

One branch = one logical unit of work. Before pushing the branch or opening a PR, run the
`pr-reviewer` agent against the local diff (`git diff main...HEAD`) and fix blocking
findings — the PR should already be clean when it opens, not fixed up after with a
follow-up commit. Every change reaches `main` only via a PR that is approved (CI green +
quality gates passing); delete the branch after merge. Use the `start-task` skill to get on
the right branch.

## Project skills (`.claude/skills/`)

- `/start-task` — before any code: ensure clean, up-to-date `main`, then create the
  correctly named branch (routes features to `/speckit-specify`, else `<type>/<slug>`).
- `/go-best-practices` — apply before writing or reviewing Go: layer placement, error
  handling, context, errgroup concurrency, slog, constructors, golangci-lint compliance.
- `/write-go-tests` — write behavior-focused tests: table-driven + testify, uber-go/mock
  generated mocks, testcontainers Postgres, LLM golden fixtures, >=80% usecase/domain.
- `/commit` — craft Conventional Commits with architecture-aligned scopes and no AI/co-author
  attribution; commit only when asked, on a task branch (never `main`).

These are also model-invocable: they load automatically when their task comes up.

## Project agents (`.claude/agents/`)

- `pr-reviewer` — read-only senior review of a diff/PR against the constitution, Clean
  Architecture layer rules, contract-first OpenAPI, Go best practices, and test rigor, with
  a focus on flagging unnecessary/dead code. Run it on the branch diff before opening a PR
  (not after) and fix blocking findings first.

## Sprint status

Phase 1 (backend) is functionally complete and deployed. All 5 endpoints, Postgres
persistence, Redis-backed rate limiting/caching/idempotency, CORS, and full OTel
instrumentation are done — see `main`'s history (`git log --oneline main`) for the
PR-by-PR record rather than trusting a sprint-number summary here, since this section
drifts. Sprint 7 (deploy) is done; Grafana Cloud is wired (issue #39 —
`OTEL_EXPORTER_OTLP_ENDPOINT`/`OTEL_EXPORTER_OTLP_HEADERS` set as a GitHub Environment
variable/secret and passed through in `deploy.yml`'s Cloud Run `env_vars`; no Go code
change needed, `ADR-0007`'s gate already reads them). Backend Sentry (issue #40) is still
open. Live on Cloud Run at
`https://pricing-optimizer-api-hnzq7nvuqq-uc.a.run.app`, deployed automatically via
`.github/workflows/deploy.yml` on every push to `main` (migrations via `goose`, then
build/push/deploy, then an idempotent `run.invoker` binding for `allUsers` since this API
is called directly from the browser — see `ADR-0013`). Neon (Postgres) and Upstash
(Redis, TLS required — `REDIS_TLS_ENABLED`) are both provisioned and verified with a real
production `POST /v1/analyze` round-trip, not just `/v1/healthz`. Issue #31 (self-hosted
browser service) remains explicitly parked. Still open: a GCP billing budget alert was
discussed but not confirmed configured — check before assuming it's done.
